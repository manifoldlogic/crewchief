#!/usr/bin/env node
// Bridge CJS bin to ESM entrypoint
const { pathToFileURL } = require('node:url');
const { resolve } = require('node:path');
const entryUrl = pathToFileURL(resolve(__dirname, '../dist/index.js')).href;
import(entryUrl);

