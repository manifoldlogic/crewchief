import React from 'react';
import { LayoutDemo as LayoutDemoComponent } from '../components/layout/LayoutDemo';

/**
 * Layout Demo Page
 * 
 * This page showcases the new layout components built for TICKET-013.
 * It demonstrates all the features including responsive design, 
 * split panes, keyboard navigation, and accessibility features.
 */
const LayoutDemo: React.FC = () => {
  return <LayoutDemoComponent />;
};

export default LayoutDemo;