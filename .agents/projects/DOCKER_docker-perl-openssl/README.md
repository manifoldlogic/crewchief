# DOCKER_docker-perl-openssl - Docker Perl OpenSSL Fix

**Status:** 🔄 In Progress (1 ticket)
**Goal:** Add Perl to Docker image to support vendored OpenSSL compilation

## Overview

This project adds Perl to the Docker build environment to enable vendored OpenSSL compilation in cross-platform Rust builds.

## Quick Links

- [Tickets](./tickets/) - Active work tickets

## Context

Required for BINPKG project's cross-compilation support using vendored OpenSSL.
