Update the README to show the docker command for mcp setup, like in my .mcp.json file, instead of the npx commands that are having issues because they try to start over each time. The npx commands are used for setup, scanning, and watching. THe Docker command is used in the MCP server configuration.


THere is an issue where the watch command always shows only one file changed, even when multiple files are changed. 