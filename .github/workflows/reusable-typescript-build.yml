name: Reusable TypeScript Build

on:
  workflow_call:
    inputs:
      workspace_filter:
        description: 'pnpm filter for packages to build (e.g., ./packages/*, @crewchief/cli...)'
        required: false
        type: string
        default: './packages/*'

      artifact_name:
        description: 'Name for dist artifacts'
        required: false
        type: string
        default: 'typescript-dist'

    outputs:
      artifact_name:
        description: 'Name of uploaded artifact'
        value: ${{ jobs.build.outputs.artifact_name }}

jobs:
  build:
    name: Build TypeScript
    runs-on: blacksmith-4vcpu-ubuntu-2404

    outputs:
      artifact_name: ${{ inputs.artifact_name }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm -r --filter='${{ inputs.workspace_filter }}' build

      - name: Verify dist directories exist
        run: |
          # Check that at least one dist/ directory was created
          if ! find packages -name "dist" -type d | grep -q .; then
            echo "ERROR: No dist/ directories found after build"
            exit 1
          fi

          # List all dist directories for verification
          find packages -name "dist" -type d

      - name: Create artifact structure
        run: |
          # Create a temporary directory with only dist/ contents
          mkdir -p /tmp/artifact
          find packages -name "dist" -type d | while read dist_dir; do
            # Get the package directory (e.g., packages/vscode-maproom)
            package_dir=$(dirname "$dist_dir")
            # Create the same structure in /tmp/artifact
            mkdir -p "/tmp/artifact/$package_dir"
            # Copy dist contents preserving structure
            cp -r "$dist_dir" "/tmp/artifact/$package_dir/"
          done
          ls -la /tmp/artifact/packages/

      - name: Upload dist artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact_name }}
          path: /tmp/artifact/packages
          if-no-files-found: error
          retention-days: 7
          include-hidden-files: false
