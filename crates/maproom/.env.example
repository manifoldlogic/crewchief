# Maproom local configuration

# DSN format: postgres://<username>:<password>@<host>:<port>/<database>
DATABASE_URL=postgres://REPLACE_WITH_USERNAME:REPLACE_WITH_PASSWORD@localhost:5432/maproom

# Optional logging
RUST_LOG=info,crewchief_maproom=info
